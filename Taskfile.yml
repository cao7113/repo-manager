# https://taskfile.dev/docs/guide
# https://taskfile.dev/reference/templating/
version: '3'
tasks:
  default: |
    go vet ./...
    go test ./...

  b: |
    go build -ldflags "-X main.version=$(cog get-version --tag --fallback 0.0.0)" -o bin/repo ./cmd/repo
